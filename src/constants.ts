export enum Collections {
  PLAYER = "player",
  STATS = "stats",
  INVENTORY = "inventory",
  CLASS = "class",
}

export enum Fields {
  XP = "xp",
  NAME = "name",
  CLASS = "class",
}

export enum Methods {
  UPDATE = "update",
  FIND_ONE = "findOne",
  SAVE = "save",
}

export enum Classes {
  WARRIOR = "warrior",
  MAGE = "mage",
}

export enum Commands {
  SCENE_START = "scene:start",
  COMMAND_START = "start",
  COMMAND_YES = "yes",
  COMMAND_NO = "no",
  COMMAND_WARRIOR = "warrior",
  COMMAND_MAGE = "mage",
}
